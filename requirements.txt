# Core ML
torch>=2.2
transformers>=4.48,<5.0  # 4.48 adds the modernbert architecture (answerdotai/ModernBERT-base)
datasets>=2.20
accelerate>=0.33
evaluate>=0.4
sentencepiece>=0.2
protobuf>=4.25
seqeval>=1.2

# Retrieval (RAG)
sentence-transformers>=3.0
faiss-cpu>=1.8
groq>=1.0  # generator LLM client for the Phase 5 pipeline; SDK is stable post-1.0

# API + demo (Phase 6)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0  # already pulled by fastapi; pinned since we author v2 models against it
gradio>=5,<6  # Step 6.2 demo UI. Floor 5: gradio 4.x uses the old starlette
              # TemplateResponse(name, ...) signature and 500s on this repo's modern
              # starlette. Cap <6: gradio 6.x forces huggingface-hub>=1.0, which conflicts
              # with transformers' huggingface-hub<1.0 pin.

# Datos y análisis
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
nltk>=3.9

# Notebooks
jupyter
ipykernel

# Utilidades
python-dotenv>=1.0
huggingface_hub>=0.24
requests>=2.32
tqdm

# Dev / CI
ruff>=0.6
black>=24.8
pytest>=8.3